services:
  - type: web
    name: fantasy-transliterator
    runtime: docker
    plan: free          # change to 'starter' or higher for production
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
